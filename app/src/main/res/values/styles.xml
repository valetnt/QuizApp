<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Header_1" parent="AppTheme">
        <item name="android:textColor">@color/colorYellow</item>
        <item name="android:textSize">@dimen/mainHeaderTextSize</item>
        <item name="android:fontFamily">casual</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Header_2" parent="AppTheme">
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <item name="android:textSize">@dimen/secondHeaderTextSize</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="QuestionHeader" parent="AppTheme">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textSize">@dimen/questionHeaderTextSize</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="QuestionText" parent="AppTheme">
        <item name="android:textColor">@color/colorText</item>
        <item name="android:textSize">@dimen/questionTextSize</item>
    </style>

    <style name="RadioButton" parent="AppTheme">
        <item name="android:textColor">@color/colorText</item>
        <item name="android:textSize">@dimen/questionTextSize</item>
    </style>

    <style name="SubmitButton">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:background">@color/colorPrimaryDark</item>
        <item name="android:textSize">@dimen/questionTextSize</item>
    </style>

    <style name="ToastScore" parent="AppTheme">
        <item name="android:textAllCaps">true</item>
        <item name="android:textSize">@dimen/toastScoreTextSize</item>
    </style>

    <style name="ToastMessage" parent="AppTheme">
        <item name="android:textColor">@color/colorText</item>
        <item name="android:textSize">@dimen/toastMessageTextSize</item>
    </style>

</resources>

